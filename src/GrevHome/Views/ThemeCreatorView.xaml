<UserControl x:Class="GrevHome.Views.ThemeCreatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GrevHome.Views"
             Focusable="False">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="72,38" MaxWidth="1040">
                <TextBlock Text="Theme Creator" FontSize="40" FontWeight="SemiBold" />
                <TextBlock Text="Themes recolor the whole Grev Home shell - buttons, tiles, cards and role badges. Editing previews live on this page and everywhere else in the shell; nothing is kept unless you save it."
                           Margin="0,6,0,22"
                           Foreground="{DynamicResource MutedBrush}"
                           TextWrapping="Wrap" />

                <Border Padding="16" Margin="0,0,0,18" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="12">
                    <StackPanel>
                        <TextBlock x:Name="ThemeScopeTitle" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock x:Name="ThemeScopeDescription" Margin="0,4,0,10" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" />
                        <WrapPanel>
                            <Button x:Name="SwitchScopeButton" Content="Manage Machine Default" MinWidth="210" Margin="0,0,10,0" Click="SwitchScope_Click" Visibility="Collapsed" />
                            <Button x:Name="UseMachineDefaultButton" Content="Use Machine Default" MinWidth="190" Click="UseMachineDefault_Click" />
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <TextBlock Text="YOUR THEMES" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource MutedBrush}" />
                <WrapPanel x:Name="ThemeGalleryPanel" Margin="0,8,0,22" />

                <Border Padding="24" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="16">
                    <StackPanel>
                        <TextBlock Text="EDITING" Style="{StaticResource ShellModalEyebrowStyle}" />
                        <Grid Margin="0,10,0,18">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="ThemeNameBox" MinHeight="48" Padding="12" FontSize="18" IsReadOnly="True" IsTabStop="False" />
                            <Button Grid.Column="1" Content="Rename" MinWidth="150" Margin="10,0,0,0" Click="RenameTheme_Click" />
                        </Grid>

                        <WrapPanel x:Name="ColorFieldsPanel" />

                        <TextBlock Text="LIVE PREVIEW" Margin="0,20,0,8" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource MutedBrush}" />
                        <Border Padding="18" Background="{DynamicResource WindowBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="12">
                            <WrapPanel>
                                <Button Style="{StaticResource SharpTileButtonStyle}" Width="150" Height="90" Margin="0,0,12,0" Content="Tile" FontSize="18" />
                                <Button Content="Button" Width="130" Height="48" Margin="0,0,12,0" />
                                <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="14" VerticalAlignment="Center">
                                    <StackPanel>
                                        <TextBlock Text="Card" FontWeight="SemiBold" />
                                        <TextBlock Text="Muted detail text" Foreground="{DynamicResource MutedBrush}" FontSize="12" />
                                    </StackPanel>
                                </Border>
                                <TextBlock Text="Admin" Margin="12,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource AdminRoleBrush}" FontWeight="SemiBold" />
                                <TextBlock Text="Standard" Margin="12,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource StandardRoleBrush}" FontWeight="SemiBold" />
                                <TextBlock Text="Guest" Margin="12,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource GuestRoleBrush}" FontWeight="SemiBold" />
                            </WrapPanel>
                        </Border>

                        <TextBlock x:Name="ContrastWarningText"
                                   Margin="0,10,0,0"
                                   Foreground="#F2C94C"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   Visibility="Collapsed" />

                        <WrapPanel Margin="0,20,0,0">
                            <Button x:Name="SaveThemeButton" Content="Save Theme" MinWidth="200" Margin="0,0,10,10" Click="SaveTheme_Click" />
                            <Button x:Name="SaveAsNewThemeButton" Content="Save as New Theme" MinWidth="200" Margin="0,0,10,10" Click="SaveAsNewTheme_Click" />
                            <Button Content="Export This Theme" MinWidth="200" Margin="0,0,10,10" Click="ExportTheme_Click" />
                            <Button x:Name="DeleteThemeButton" Content="Delete This Theme" Style="{StaticResource ShellModalDangerActionButtonStyle}" MinWidth="200" Margin="0,0,10,10" Click="DeleteTheme_Click" />
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <WrapPanel Margin="0,18,0,0">
                    <Button x:Name="NewThemeButton" Content="New Theme" MinWidth="150" Margin="0,0,10,10" Click="NewTheme_Click" />
                    <Button x:Name="ImportThemeButton" Content="Import Theme" MinWidth="150" Margin="0,0,10,10" Click="ImportTheme_Click" />
                    <Button Content="Back" MinWidth="130" Margin="0,0,10,10" Click="Back_Click" />
                </WrapPanel>
                <TextBlock x:Name="StatusText" Margin="0,8,0,0" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </ScrollViewer>
        <local:ControllerQwertyKeyboard x:Name="KeyboardOverlay" Panel.ZIndex="100" />
    </Grid>
</UserControl>
