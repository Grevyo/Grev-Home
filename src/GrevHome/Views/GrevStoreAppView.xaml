<UserControl x:Class="GrevHome.Views.GrevStoreAppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="False">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="72,38" MaxWidth="1180">
            <TextBlock Text="Grev Store" FontSize="14" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" />

            <Border Margin="0,10,0,0"
                    Padding="28"
                    Background="#11151E"
                    BorderBrush="#2B3344"
                    BorderThickness="1"
                    CornerRadius="18">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Border x:Name="AppArtworkHost"
                            Margin="0,0,24,0"
                            Background="Transparent" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="AppNameText" Text="App" FontSize="38" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                        <TextBlock x:Name="AppTypeText" Margin="0,6,0,0" FontSize="15" Foreground="{StaticResource MutedBrush}" />
                        <TextBlock x:Name="OwnershipSummaryText" Margin="0,10,0,0" FontSize="14" TextWrapping="Wrap" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="24,0,0,0" VerticalAlignment="Center" MinWidth="190">
                        <Button x:Name="DownloadButton" Content="Download" Height="54" Click="Download_Click" />
                        <Button x:Name="OpenButton" Content="Open" Height="54" Margin="0,8,0,0" Visibility="Collapsed" Click="Open_Click" />
                        <Button x:Name="UninstallButton" Content="Uninstall" Height="54" Margin="0,8,0,0" Visibility="Collapsed" Click="Uninstall_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <UniformGrid Columns="4" Margin="0,16,0,0">
                <Border Margin="0,0,6,0" Padding="18" Background="#11151E" BorderBrush="#2B3344" BorderThickness="1" CornerRadius="12">
                    <StackPanel>
                        <TextBlock Text="INSTALL TYPE" FontSize="11" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" />
                        <TextBlock x:Name="InstallTypeText" Margin="0,7,0,0" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
                <Border Margin="6,0" Padding="18" Background="#11151E" BorderBrush="#2B3344" BorderThickness="1" CornerRadius="12">
                    <StackPanel>
                        <TextBlock Text="APP DATA" FontSize="11" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" />
                        <TextBlock x:Name="DataTypeText" Margin="0,7,0,0" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
                <Border Margin="6,0" Padding="18" Background="#11151E" BorderBrush="#2B3344" BorderThickness="1" CornerRadius="12">
                    <StackPanel>
                        <TextBlock Text="CONTROLLER" FontSize="11" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" />
                        <TextBlock x:Name="ControllerSupportText" Margin="0,7,0,0" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
                <Border Margin="6,0,0,0" Padding="18" Background="#11151E" BorderBrush="#2B3344" BorderThickness="1" CornerRadius="12">
                    <StackPanel>
                        <TextBlock Text="STORE CATEGORY" FontSize="11" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" />
                        <TextBlock x:Name="CategoryText" Margin="0,7,0,0" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.05*" />
                    <ColumnDefinition Width="0.95*" />
                </Grid.ColumnDefinitions>

                <Border Margin="0,0,8,0" Padding="24" Background="#11151E" BorderBrush="#2B3344" BorderThickness="1" CornerRadius="14">
                    <StackPanel>
                        <TextBlock Text="ABOUT THIS APP" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" />
                        <TextBlock x:Name="DescriptionText"
                                   Margin="0,12,0,0"
                                   FontSize="16"
                                   LineHeight="24"
                                   TextWrapping="Wrap" />

                        <TextBlock Text="INSTALLATION &amp; OWNERSHIP" Margin="0,24,0,0" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" />
                        <TextBlock x:Name="OwnershipDetailText" Margin="0,10,0,0" FontSize="14" TextWrapping="Wrap" Foreground="#D7DCE7" />
                        <TextBlock x:Name="InstallPathText" Margin="0,8,0,0" FontSize="12" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Margin="8,0,0,0" Padding="24" Background="#11151E" BorderBrush="#2B3344" BorderThickness="1" CornerRadius="14">
                    <StackPanel>
                        <TextBlock Text="GREV HOME INTEGRATION" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" />
                        <StackPanel x:Name="IntegrationsPanel" Margin="0,8,0,0" />
                    </StackPanel>
                </Border>
            </Grid>

            <Border Margin="0,16,0,0" Padding="22" Background="#11151E" BorderBrush="#2B3344" BorderThickness="1" CornerRadius="14">
                <StackPanel>
                    <TextBlock Text="INSTALLATION STATUS" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" />
                    <TextBlock x:Name="InstallationStateText" Margin="0,10,0,0" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
                    <TextBlock x:Name="InstallationMetadataText" Margin="0,6,0,0" FontSize="12" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" />
                    <TextBlock x:Name="StatusText" Margin="0,12,0,0" FontSize="13" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
